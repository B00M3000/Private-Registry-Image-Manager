deployment:
  autoCleanup: false
  dnsCheck: true
  pushLatest: true
  tagStrategy: git_commit
docker:
  buildArgs: {}
  buildContext: .
  localImageName: Private-Registry-Image-Manager
project:
  dockerfile: Dockerfile
  name: Private-Registry-Image-Manager
registry:
  repository: Private-Registry-Image-Manager
  url: registry.example.com
